## 1. Specification Updates
- [x] 1.1 Update pivot engine requirement to PivotModel-based engine and deduped subtotals/grand totals.
- [x] 1.2 Replace valuePosition + render model requirements with rendering helper coverage and remove row-mode support.
- [x] 1.3 Refresh style service requirement (level-based headers, grand total color) and add worksheet-capacity safeguard requirement.

## 2. Validation
- [x] 2.1 Run `openspec validate update-pivot-model-v3 --strict` and resolve findings.

