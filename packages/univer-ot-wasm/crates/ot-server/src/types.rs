// Types used by the OT server.
// Socket.IO-specific types have been removed — all WebSocket communication
// is now handled by the ws-gateway service.
// HTTP API types are defined in handlers/api.rs.
